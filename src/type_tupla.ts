//Tupla não existe no JS, somente no TS

let aluno: [string, number] //Precisa seguir está ordem na hora de por os valores

aluno = ['Taylan', 19]

console.log(aluno)

console.log('======')


let statusPedido: [string, string, string]

statusPedido = ['Em produção', 'Saiu para entrega', 'Pedido entregue']

console.log(statusPedido);
    